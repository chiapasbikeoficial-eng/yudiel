# Kali WiFi Auditor - Environment Variables
# These variables are used by the backend server

# Set to production to disable Vite dev middleware
NODE_ENV=development

# The interface to use for Wi-Fi operations
DEFAULT_INTERFACE=wlan0mon

# Port for the web interface
PORT=3000
